export default function Main({ children }) {
  return (
    <main className="relative overflow-hidden transition-all font-Inter bg-veryLightGrey dark:bg-darkBlue">
      {children}
    </main>
  );
}
